## What changed

<!-- The change itself, in plain terms. -->

## Why

<!-- The reason it was needed. Not "what" restated — the problem, constraint, or
     decision behind it. A reviewer should be able to disagree with the reasoning. -->

## How to verify

<!-- What you actually ran or observed, and what a reviewer can run to see the same.
     If something is unverified, say so here rather than leaving it implied. -->

---

<!-- Ready to land? Use this repo's merge gate — add the `queue` label (merge
     queue / Mergify tiers) or hand it to the coordinator session (serialized
     tier). The gate re-tests against current main before landing; merging
     directly skips that re-test — reserve it for when the gate is down. -->
